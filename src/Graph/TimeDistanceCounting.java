package Graph;

public class TimeDistanceCounting implements EdgeDistanceCounting{
    @Override
    public double calculateWaitingTime(int startTime, double previousEndTime, String line, String line2) {
        return 0;
    }
}
